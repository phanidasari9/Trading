# Skip interactive "Welcome to Streamlit" email prompt on first run (required for
# Streamlit Community Cloud / non-interactive deploys — otherwise the server never
# binds to the port and health checks fail).
[general]
email = ""
